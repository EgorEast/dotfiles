---
agent: build
description: Migrate from Styled Components
---

# migrate-from-styled-components

migrate from styled-components to module css. Use also css vsriables from app/layout/cssVariables and use css nesting. wrap every antd class to :global(). make migrate for
